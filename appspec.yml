version: 0.0
os: linux 

files:
  - source: /
    destination: /home/ubuntu/nodejs
    
hooks:
  BeforeInstall:
    - location: Dependency_Scripts/rmBeforeInstall.sh
      timeout: 300
      runas: ubuntu

  ApplicationStart:
    - location: Dependency_Sscripts/startApplication.sh
      timeout: 300
      runas: ubuntu
